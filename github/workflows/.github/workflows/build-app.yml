name: Build App APK
on:
  push:
    paths:
      - 'app-settings.json'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Preparing Build
        run: echo "Starting APK generation for the provided URL..."
        
